#ifndef __PRINTFUNC__
#define __PRINTFUNC__

#define PRETTY 20

// print the array into file
void print_array_to_file(int * array, int arraySize, char * fileName, double running_average_time);

#endif
